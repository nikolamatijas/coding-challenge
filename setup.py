from roas_calculator.roas_calculator import RoasCalculator
from setuptools import setup, find_packages

setup(
    name='roas_calculator',
    version='0.1',
    packages=find_packages(include=['roas_calculator.*'])
)
